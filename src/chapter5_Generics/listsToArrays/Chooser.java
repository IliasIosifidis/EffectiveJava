package chapter5_Generics.listsToArrays;

public class Chooser {
  /*
    private final Object[] choiseArray;

  public Chooser(Collection choiseArray) {
    choiseArray = choices.toArray();
  }

  public Object choose(){
    Random random = ThreadLocalRandom.current();
    return choiseArray[random.nextInt(choiseArray.length)];
  }
   */
  }
